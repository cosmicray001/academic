    #include <bits/stdc++.h>
    using namespace std;
    int main(){
    	int a, b, t;
    	for(scanf("%d", &t); t--; ){
    		cin >> a >> b;
    		printf("%s\n", (a == b) ? "YES" : "NO");
    	}
    	return 0;
    }