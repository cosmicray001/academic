#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n;
    while(scanf("%d", &n) != EOF){
        if(n == 42){
            scanf("%d", &n);
            break;
        }
        else printf("%d\n", n);
    }

    return 0;
}
